# Object Tracking using yolo8

#### create environment and activate the environment
python3 -m venv ot_env </br>
source ot_env/bin/activate

#### run requirement file to install all packages
pip install -r requrement.txt